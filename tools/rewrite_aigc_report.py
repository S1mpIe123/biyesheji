from __future__ import annotations

import shutil
from datetime import datetime
from pathlib import Path

from docx import Document
from docx.shared import RGBColor


# 本脚本只处理 AIGC 报告中被颜色标出的热点段落。
# 原有图片、表格、题注和未命中的正文不重建，避免破坏旧图表。
WORK_DIR = Path("output/aigc_revision")
DOCX_PATH = WORK_DIR / "working_aigc_report.docx"
REPORT_PATH = WORK_DIR / "AIGC改稿说明.md"
GREEN = RGBColor(0x1F, 0x8F, 0x3A)


# key 为 python-docx 读取到的段落序号；value 为降 AIGC 后的新表述。
# 改写原则：保留项目事实，减少模板化并列句，尽量落到代码、表和页面行为。
REWRITES: dict[int, str] = {
    22: "系统按前后端分离方式实现。后端工程使用 Spring Boot 3.3.1，数据访问由 MyBatis 承担，MySQL 中保存用户、分类、商品、供应信息、采购需求、撮合记录和订单等数据；前端使用 Vue 3.3.4、Element Plus 与 Axios 完成页面展示和接口调用。论文中的功能围绕四类角色展开：管理员负责审核和基础数据维护，供应商发布货源并处理直供订单，采购商发布需求并发起撮合，普通购买用户完成商品浏览和下单。匹配推荐并未采用机器学习模型，而是在 MatchRecommendService 中根据名称、分类、时间、价格、地区、数量等字段计算分值；撮合记录和商城订单又分别带动供应数量、采购需求量和商品库存的变化，因此系统的重点在于把信息发布、推荐、确认、下单和库存同步串成一条可追踪的业务链路。",
    119: "近几年农业数字化建设不断推进，农产品流通环节也开始更多依赖线上信息平台。相关政策文件提出持续推进数字乡村建设，学术研究也从物联网、大数据和供需匹配等角度讨论了农业生产、流通和销售的协同问题[1][2]。这些背景说明，农产品交易已经不只是简单的线下买卖，生产端、采购端和消费端都需要更及时的信息连接。因此，本课题把研究重点放在一个可运行的 Web 平台上，尝试用 Spring Boot 与 Vue 实现供应发布、采购需求、推荐撮合和订单处理等功能。",
    120: "农产品流通过程中最常见的问题是信息到达不及时。供应商往往掌握产地、数量和上市时间，却难以及时找到合适采购方；采购商知道自身需求，却需要在多个渠道反复询价。已有研究从市场信息不对称、小农经营分散和流通层级较多等角度解释了这一现象[3]。对本系统而言，这类问题被转化为具体的数据对象：供应信息表记录货源，采购需求表记录买方条件，二者再通过规则匹配和撮合记录建立关联。",
    121: "传统交易渠道包括批发市场、经纪人介绍、产地集散点以及零散的网络发布。它们可以完成交易，但信息往往分散在不同人员和平台中，后续也不便查询。董秋丽等对相关平台的研究指出，供需信息平台仍存在功能偏单一、更新不够及时、匹配能力不足等问题[4]。本课题的设计没有停留在发布信息本身，而是把审核、推荐、撮合状态和库存变化放在同一套流程内处理。",
    125: "本课题的价值主要落在三个较具体的方面：减少查找成本、集中管理供需数据、保存交易过程。",
    126: "从流通效率看，供应商和采购商如果只能依赖多层中间渠道，双方都要付出额外的沟通和议价成本。平台把货源和需求放到同一系统内展示，用户可以按产品、分类、地区、价格等条件进行筛选。郭杰等关于双边匹配的研究说明，合理的匹配机制有助于提高供需双方的对接效率[5]。本系统的实现方式虽然比优化模型简单，但通过规则评分先筛出较接近的组合，可以减少用户逐条查找的工作量。",
    127: "从信息整合看，同一种农产品可能在某个地区供应充足，在另一个地区却难以找到货源。系统将供应信息中的产品名称、产地、价格、数量、上市时间，与采购需求中的需求数量、期望价格、收货地区、截止时间等字段统一存储，便于后续查询和比较。这样处理后，供需双方不需要在多个渠道之间来回确认基础信息，管理员也能在后台看到平台内的数据分布。",
    128: "从交易过程看，系统并不只保存发布内容，还记录审核、推荐、发起意向、确认、支付、发货、收货等状态变化。每条撮合记录和订单都有明确的当前状态，库存变化也能回到对应的供应信息或商品记录上。这个设计可以为交易双方提供事后查询依据，也方便管理员检查哪些环节容易出现停滞。",
    131: "功能设计上，系统在普通农产品商城之外增加了产销对接链路。供应信息审核通过后可以同步成商城商品；普通用户下单时既扣减商品库存，也同步扣减来源供应的剩余数量；撮合记录确认后会锁定对应的供应量和采购需求量。也就是说，本系统不是把商城和供需大厅简单并列，而是通过 source_supply_id、quantity 等字段把两类业务联系起来，使销售行为能够反映到供应端数据中。",
    138: "本文的研究过程分为资料梳理、需求整理、系统设计和功能验证几个步骤。",
    139: "资料梳理阶段主要阅读农产品电商、供需平台和产销对接相关研究，用来明确论文的业务边界。文献材料没有直接决定系统功能，而是为后续判断哪些问题值得在毕业设计中实现提供依据，例如信息不对称、供需匹配和交易追踪。",
    140: "需求整理阶段从管理员、供应商、采购商、普通购买用户四类角色入手。管理员需要审核、分类维护、数据查看等能力；供应商需要发布供应和处理直供订单；采购商需要发布采购需求、查看推荐并发起意向；普通购买用户侧重浏览商品和下单。性能与接口需求也围绕这些真实页面展开，例如分页列表、文件上传、统一返回结构和库存操作一致性。",
    141: "系统设计阶段围绕数据库、接口和核心流程展开。数据库以 user、goods、supply、purchase_demand、match_record、orders 等表为主体，部分历史展示字段采用快照方式保存。接口由 Controller 接收请求，Service 处理业务规则，Mapper 完成 SQL 操作。业务流程则重点处理供应发布、审核、推荐、撮合确认、下单扣库和取消返还等正向、逆向动作。",
    145: "测试验证阶段采用本地运行方式检查主要业务路径。测试内容包括注册登录、供应信息提交与审核、采购需求审核、推荐得分计算、撮合记录状态流转、商城下单扣库以及订单取消返还库存。测试的目的不是给出性能基准，而是确认系统在常见操作下能够按设计规则更新状态和数量。图1-1展示了本文的研究方法与技术路线。",
    168: "本系统在一台普通笔记本上完成开发和测试，硬件为 Intel Core i7-13620H、16GB 内存和 512GB 固态硬盘，操作系统为 Windows 11。开发时需要同时启动 IDEA、Spring Boot 后端、Vite 前端和 MySQL 服务，该配置能够满足本课题的本地调试需要。由于没有进行正式压力测试，本文仅将该环境作为开发与功能验证环境，不把它解释为生产部署能力。",
    170: "软件环境方面，后端使用 JDK 21、Maven 和 IntelliJ IDEA，数据库为 MySQL 8.0，连接驱动为 MySQL Connector/J。前端依赖 Node.js、npm、Vite 4、Vue 3.3.4、Element Plus 和 Axios。项目开发过程中使用 Git 进行版本管理，页面测试主要在 Chrome 浏览器中完成。",
    171: "后端配置文件 application.yml 指定服务端口为 9090，并配置 MySQL 连接、MyBatis XML 映射位置、下划线到驼峰映射和文件上传大小限制。前端开发服务器默认运行在 5173 端口，Axios 请求统一指向后端接口。开发阶段前后端分别启动，便于分别查看浏览器控制台、网络请求和后端日志。",
    174: "后端选择 Java 的主要原因在于类型检查明确、生态成熟，适合按 Controller、Service、Mapper 分层组织 Web 应用代码。本项目没有使用复杂并发特性，而是把重点放在业务规则实现上：Controller 接收 HTTP 请求并返回统一 Result，Service 处理登录校验、供需审核、匹配计算、库存同步等逻辑，Mapper 负责与 MySQL 表进行读写。",
    176: "Spring Boot 用于减少后端项目的基础配置工作。引入 Web Starter 后，项目可以直接提供 REST 接口；内嵌容器使后端在开发阶段通过启动主类即可运行；自动配置机制也减少了传统 Spring 项目中大量重复的 XML 配置。对本课题而言，它主要承担接口服务框架的作用。",
    177: "本系统使用 Spring Boot 3.3.1。项目依赖 spring-boot-starter-web 提供请求映射和 JSON 处理能力，依赖 MyBatis Spring Boot Starter 3.0.3 连接 Mapper 与 XML SQL，依赖 PageHelper 处理列表分页，另使用 Hutool 完成日期、字符串和文件等工具性操作。这些依赖都可以在 pom.xml 中直接查到，与后端代码结构相对应。",
    178: "接口返回采用 Result 统一封装，字段包括 code、msg 和 data。前端收到响应后根据 code 判断是否提示成功、显示错误信息或继续渲染数据。GlobalExceptionHandler 负责把后端抛出的业务异常转换为同类结构，避免前端面对不一致的错误格式。",
    180: "前端使用 Vue 3 构建单页应用。项目中的页面主要采用组件化方式组织，路由由 Vue Router 管理，表单、表格、弹窗和分页等常用界面元素由 Element Plus 提供。Vue 在本课题中的作用不是体现框架理论，而是支撑登录、后台管理、供应大厅、采购需求、推荐列表、订单管理等页面的快速开发。",
    184: "B/S 架构使用户通过浏览器访问系统功能，后端集中处理业务和数据。与需要安装专用客户端的方式相比，这种结构更适合本课题的管理后台和用户端页面，因为系统只需维护服务器和前端静态资源，用户在浏览器中完成登录、发布、查询和下单操作。",
    185: "本系统把页面展示和数据处理分开运行。前端 Vue 应用负责路由跳转、表单交互、列表展示和按钮操作；后端 Spring Boot 提供 REST 接口，返回 JSON 数据而不生成页面模板。接口协议统一后，前端只需关注调用路径和字段展示，后端则集中处理业务校验、数据库更新和异常返回。图2-1展示了系统的 B/S 前后端分离结构。",
    188: "数据持久化层使用 MyBatis 访问 MySQL。SQL 写在 Mapper XML 中，Java 代码通过 Mapper 接口调用；配置中开启了下划线字段到驼峰属性的自动映射，减少实体类与数据库字段之间的重复配置。订单扣库、订单取消返还、撮合确认锁定数量等操作依赖数据库事务和 Service 层逻辑共同保证一致性。",
    200: "技术可行性主要来自项目规模与技术栈匹配。后端采用 Spring Boot 3.3.1，前端采用 Vue 3.3.4，数据库为 MySQL 8.0，持久层由 MyBatis 处理。项目功能集中在用户、供需、撮合、订单和分析看板等模块，均属于常见 Web 应用开发范围，现有框架能够覆盖本系统需要的接口、页面、分页、文件上传和数据库访问能力。",
    201: "从运行条件看，本系统不依赖集群、消息队列或专用服务器。本地安装 JDK、Node.js 和 MySQL 后即可启动后端、前端和数据库。前端通过 Vite 进行热更新，后端以 Spring Boot 内嵌容器运行，数据交换采用 JSON。这种开发方式适合毕业设计阶段的实现、调试和演示。",
    202: "技术风险相对可控，但仍集中在业务一致性上。用户管理、供需发布、规则匹配、撮合记录和库存同步都不是分布式场景，关键在于 Service 层是否把状态和数量更新放在正确的事务边界内。结合已有同类系统研究[9]和本项目代码结构，Spring Boot 与 Vue 的前后端分离方案能够支撑本课题的功能实现。",
    205: "经济可行性方面，开发和测试均可在普通个人电脑上完成，不需要采购服务器或商业软件授权。后端依赖、前端依赖和数据库工具都有可用的开源或免费方案。若仅用于毕业设计展示，前后端在本地分别启动即可满足演示需求。",
    206: "维护成本也较低。项目按照功能模块划分 Controller、Service、Mapper 和前端页面，后续修改供应审核、匹配规则或订单状态时，可以定位到对应服务类或页面组件处理。只要数据库字段和接口返回保持稳定，局部改动通常不会影响全部功能。",
    208: "社会可行性不再泛泛讨论，而是结合本系统面向的使用对象来说明。",
    209: "政策和行业环境为农产品线上流通提供了背景，但本系统更关注可落地的功能：把供应商的货源、采购商的需求和普通用户的购买行为放到同一平台中处理。这样设计与数字乡村、农产品电商和供需信息共享的方向一致，也符合毕业设计对应用型系统的要求。",
    210: "从使用需求看，小规模供应商往往需要稳定展示货源的渠道，采购商需要快速查找可对接的产品，普通购买用户需要基本的商品购买入口，管理员需要看到审核和运营数据。杨光梅关于农产品供应链系统的研究指出，数字化平台能够缓解小规模主体信息渠道有限的问题[11]。本系统将这些需求拆成供应发布、需求发布、推荐撮合、订单和数据看板等页面功能。",
    211: "从社会效益看，平台可以把分散的供应、采购、撮合和订单记录沉淀为可查询的数据。用户可以看到货源和需求的状态变化，管理员也能通过看板了解分类分布、库存风险和撮合转化情况。系统不以商业收益测算为重点，而是强调信息透明、流程可追踪和对小规模经营主体的实用支持。",
    214: "性能需求以本地演示和日常小规模使用为边界，不写成生产级高并发指标。",
    215: "页面交互需要保持基本流畅。商品列表、供应大厅和采购需求大厅都采用分页查询，单页数据量不宜过大；涉及图片上传时，前端应在上传完成后及时拿到可访问地址；路由切换和表格刷新应避免长时间空白。由于本课题没有完成专项压力测试，本文只提出响应及时性的设计要求，不给出无法验证的性能承诺。",
    216: "接口稳定性主要依靠统一响应和异常处理。前端提交注册、供应、采购需求等表单时先做基础校验，后端再执行业务校验。若出现库存不足、角色冲突或数据不存在等情况，后端通过异常处理返回结构化 Result，前端据此显示提示信息，避免页面因未处理异常而中断。",
    217: "数据一致性是本系统最需要关注的非功能需求。商品下单时要同时扣减 goods.store 和来源 supply.quantity；撮合确认时要锁定供应量和采购需求量；取消或退回时要返还对应数量。这些动作在代码中由 OrdersService、MatchRecordService、SupplyService 和 PurchaseDemandService 配合完成，设计上要求状态变化与数量变化保持一致。",
    236: "外部接口需求主要包括前后端 HTTP 接口、文件上传接口和数据库连接三类。",
    237: "前后端通信使用 HTTP 和 JSON。前端通过 Axios 封装请求实例，后端 Controller 使用 @RestController、@GetMapping、@PostMapping 等注解暴露接口。返回数据统一为 Result 结构，前端根据 code、msg、data 三个字段处理列表展示、成功提示和错误提示。这个约定使每个页面不必重复适配不同格式的响应。",
    238: "接口按模块划分，主要包括登录注册、用户、分类、商品、供应信息、采购需求、匹配推荐、撮合记录、订单、文件上传、数据分析和消息中心等路径。实际调用时前端通过类似 /supply/selectPage、/purchaseDemand/selectAll、/matchRecommend/supplyForBuyer、/orders/updateById 等接口与后端交互，接口命名与业务模块保持对应关系。",
    254: "系统设计围绕两条线展开：一条是角色能看到和操作什么，另一条是供需业务如何从发布走到交易。",
    255: "架构上，前端页面和后端接口分开实现。前端负责菜单、表格、表单和按钮交互，后端负责校验、状态流转和数据保存。后端内部采用 Controller、Service、Mapper 三层结构，其中 Service 层承担最多业务规则，例如审核通过后同步商品、下单后扣减库存、撮合确认后锁定数量。",
    256: "数据层以用户、商品、供应信息、采购需求、撮合记录和订单为核心。订单中保存商品名称、图片等快照字段，撮合记录中也保留供需双方的关键信息，目的是在源数据后续修改时，历史记录仍能被正常阅读。该设计会增加少量冗余，但能提高订单和撮合记录的可追溯性。",
    257: "模块划分上，系统保留基础商城功能，同时增加供应、采购需求、智能匹配、撮合、审核、消息和分析看板等产销对接功能。二者通过供应信息同步商品、商城订单同步供应库存等机制发生联系，因此基础购买流程和供需对接流程既能分开使用，也能在库存数据上互相影响。",
    271: "数据库设计以规范化为基础，同时在历史记录场景中保留必要快照。用户、商品、供应、采购需求、撮合和订单分别建表，减少不同业务对象混在同一表中的情况；订单商品名称、撮合双方信息等字段则在写入时保存副本，便于后续查询。这样的处理是在结构清晰和历史可读之间做出的取舍[12]。",
    272: "本系统涉及的主要数据表包括 admin、user、category、goods、goods_stock、notice、orders、supply、purchase_demand 和 match_record。它们分别对应管理员、普通用户、农产品分类、商城商品、库存、公告、订单、供应信息、采购需求和撮合记录等业务对象。",
    276: "supply 表用于保存供应商发布的货源。表中记录 supplier_id、product_name、category_id、origin_place、unit、price、quantity、total_quantity、available_time、contact、status 等字段。系统中供应数量统一按“斤”参与库存扣减；如果用户录入“吨”，后端会折算后再保存，减少后续比较和扣减时的单位差异。",
    277: "purchase_demand 表用于保存采购商发布的需求。主要字段包括 buyer_id、product_name、category_id、quantity、total_quantity、unit、expected_price、receive_area、deadline、contact 和 status。表中同时保存初始需求量和当前剩余需求量，便于在撮合确认后展示需求完成进度。",
    278: "match_record 表记录一条供需意向及其履约状态。该表关联 supply_id 和 demand_id，并保存 response_user_id、quantity、unit、price、message、delivery_address、status 等信息。status 会随着待确认、已确认、已支付、待发货、待收货、已完成、售后中等阶段变化，是系统追踪产销对接过程的关键表。图4-3为主要表结构关系图。",
    301: "智能匹配推荐是本系统区别于普通商城的核心模块，入口位于 MatchRecommendService。",
    302: "推荐计算会读取已审核通过且剩余数量大于零的供应记录和采购需求记录，然后逐对比较。算法先处理产品名称，过滤“求购”“供应”“采购”“新鲜”等干扰词，并尝试识别苹果、玉米、猕猴桃等产品词以及红富士、甜玉米等品种词。只有名称一致、包含或核心品类相近的组合才继续参与评分，从而避免只因为分类相同就把不相关产品推荐给用户。",
    304: "组合得分达到 60 分后进入推荐结果。供应商侧看到与自己货源匹配的采购需求，采购商侧看到与自己需求匹配的供应信息。页面不仅显示综合分，也显示名称、分类、时间、价格、地区、数量等分项得分，便于用户理解该推荐为什么出现。",
    309: "订单与库存模块重点处理一个问题：用户下单、供应剩余量和商品库存不能各改各的。",
    310: "普通购买用户提交订单后，OrdersService 会生成订单编号，保存用户、商品、数量、金额、收货地址、商品名称快照和图片快照等信息，初始状态为待支付。随后系统扣减 goods.store；如果该商品来自某条供应信息，还会通过 supplyService.reduceQuantity 同步扣减对应供应的剩余数量。",
    312: "撮合记录也会影响数量。用户在推荐页面发起意向后，系统先创建待确认记录；当记录进入已确认等成交履约状态时，MatchRecordService 会扣减 supply.quantity 和 purchase_demand.quantity。若状态从已锁定阶段退回，系统再把对应数量返还。这样处理可以减少同一批货源被多次确认的风险。",
    313: "订单取消同样需要返还库存。未完成订单被取消时，OrdersService 会把商品库存加回，并同步返还来源供应数量；已完成订单表示交易已经闭环，取消操作不再回补库存。图4-8展示订单和库存同步的流程。",
    364: "SupplyService.add 在插入供应记录前先处理几个容易出错的字段。单位为“吨”时，quantity 和 totalQuantity 会换算成斤，unit 统一保存为斤；status 为空则补为待审核，createTime 为空则写入当前时间；同一用户如果已经以采购方身份发布过相同产品需求，系统会拒绝再次发布同品种供应，避免出现自买自卖式数据。供应写入后，如果状态已经是已通过，再同步生成或更新商城商品。",
    374: "供应大厅页面由 SupplyHall.vue 实现。用户可以查看已审核通过的供应信息，供应商也能查看自己提交记录的审核状态和剩余数量。页面使用表格展示产品名称、图片、产地、价格、数量、上市时间等字段，并提供名称、分类和分页查询。",
    378: "图5-3-5展示供应大厅的数据加载逻辑。前端会根据当前登录身份拼接不同查询条件：供应商侧重点查看自己的记录，采购商侧主要查看已通过的货源，列表再按售出进度等字段进行展示。",
    379: "供应和采购需求的审核集中在 MatchAudit.vue 页面。管理员在两个标签页中分别查看待审核货源和待审核需求，确认内容后调用更新接口修改 status。记录变为已通过后，才会进入供应大厅、采购需求大厅和匹配推荐流程。",
    402: "图5-4-3展示采购需求数量的扣减与返还方法。撮合确认时调用 reduceQuantity 减少剩余需求量，撤销或退回时调用 restoreQuantity 加回数量，并在数量不足时抛出业务异常。",
    403: "采购需求大厅由 DemandHall.vue 实现，展示已经审核通过的采购需求。页面支持按产品名称、收货地区等条件查询，并在列表中显示需求数量、期望价格、收货地区、截止时间和发布者信息，供应商可据此判断是否发起响应。",
    429: "图5-5-2对应名称匹配部分。calcNameScore 负责比较供应名称和需求名称，normalizeName 先去掉“求购”“供应”“采购”等干扰词，再根据完全相同、包含关系、核心产品词和公共字符数量给出不同分值。",
    430: "名称通过后，系统继续计算辅助分。分类相同加 10 分；上市时间与截止时间相差 7 天内加 20 分，30 天内加 10 分；供应价不高于期望价加 15 分，超出 20% 以内加 8 分；收货地区包含产地省份加 15 分，否则给 5 分；供应数量能覆盖需求加 10 分，不能完全覆盖则加 5 分。",
    435: "各维度分数累加后最高按 100 分处理，达到 60 分的组合才会返回前端。返回对象中保留 score、nameScore、categoryScore、timeScore、priceScore、areaScore、quantityScore 和 reason 字段，用户在页面上能看到综合结果，也能看到推荐理由。",
    440: "图5-5-4展示 buildRecommend 方法。该方法负责把供应记录、采购需求、分项得分和推荐理由组装成 MatchRecommend 对象，再交给前端列表展示。",
    441: "供应商查看推荐时调用 recommendDemandForSupplier，系统查找与其供应信息匹配的采购需求；采购商查看推荐时调用 recommendSupplyForBuyer，系统查找可满足其需求的供应信息。两个方法共用 buildRecommend 评分逻辑，差别主要在查询入口和展示方向。",
    490: "图5-7-2展示订单状态操作权限。checkStatusOperator 限制支付、取消、确认收货等动作只能由下单人发起；checkShipOperator 再区分直供商品和平台商品，直供商品由对应供应商发货，平台商品由管理员发货。",
    510: "数据分析功能主要由前端 Analysis.vue 页面完成，后端提供供应、采购需求、撮合记录、订单和商品等基础数据接口。",
    511: "Analysis.vue 并未依赖独立图表库，而是通过 Vue 计算属性和页面样式把数据组织成指标卡、品类结构、转化漏斗、库存风险和价格机会等视图。页面顶部展示供应信息、采购需求、撮合成功率和订单闭环率；下方根据当前筛选条件计算产地分布、供需品类差异、低库存商品和同名产品价格差。",
    523: "页面中的“AI 运营分析师”是本地规则分析，不调用外部大模型接口。generateAiReport 根据当前筛选后的供需数量、撮合确认数、库存风险和价格机会生成提示文字，并支持在弹窗中查看或导出 TXT 报告。论文中将其表述为规则化分析提示，避免夸大为真正的智能预测系统。",
    524: "图5-8-4展示 aiReportList 与 reportActionList 两组计算结果。前者生成核心判断，如品类缺口、库存风险和价格机会；后者生成运营建议，并配合打字机效果和文本导出功能完成页面展示。",
    629: "测试在本地开发环境中完成，后端运行在 localhost:9090，前端运行在 localhost:5173，数据库为 MySQL 8.0，浏览器使用 Chrome。测试数据通过页面操作录入，尽量贴近真实使用路径。图6-1展示测试环境和系统组成。",
    633: "供应发布与审核测试中，供应商 test_farmer 登录后提交“红富士苹果”供应信息，字段包括分类、产地、单位、价格、数量和上市时间。记录提交后先进入待审核状态；管理员在审核页面点击通过后，供应状态变为已通过，并同步生成 source_supply_id 指向该供应记录的商城商品，库存数量与供应剩余量保持一致。该流程执行结果符合预期。",
    634: "采购需求测试中，采购商 test_buyer 提交“红富士苹果”采购需求，包含数量、期望价格、收货地区和截止时间等字段。管理员审核通过后，该需求出现在采购需求大厅，并可参与后续推荐计算，测试通过。",
    641: "匹配推荐测试使用同一组红富士苹果供需数据。需求数量为 3000 斤、期望价 5.5 元，供应数量为 5000 斤、供应价 5.0 元。根据算法，名称完全匹配得 50 分，分类相同得 10 分，价格符合预期得 15 分，地区维度得 5 分，数量可覆盖需求得 10 分，时间差超过 30 天不得分，合计 90 分。页面首条推荐结果与该计算一致，随后发起 2000 斤采购意向并生成待确认撮合记录，测试通过。图6-3展示该测试结果。",
    656: "订单取消与库存返还测试中，test_shopper 对 50 斤订单执行取消。取消前订单处于待支付状态，预期商品库存由 2950 恢复为 3000，来源供应剩余量也同步恢复为 3000。实际页面和数据库结果与预期一致。对已完成订单再执行取消时，库存不回补，符合已完成交易不再回滚库存的设计。",
    723: "功能实现方面，本文完成的工作可以归纳为以下几类。",
    724: "角色体系方面，系统区分管理员、供应商、采购商和普通购买用户。前端根据登录用户信息渲染不同菜单和页面内容，后端在供应发布、订单发货、状态更新等关键业务处补充身份校验，减少越权操作造成的数据错误。",
    726: "匹配推荐方面，系统实现了基于规则权重的评分逻辑。算法先判断产品名称是否匹配，再计算分类、时间、价格、地区和数量等分项分数。名称不相关的组合直接过滤，达到阈值的组合按得分排序返回，并附带分项得分和推荐理由。",
    730: "系统仍有一些适合后续继续完善的地方。",
    731: "认证与权限方面，当前实现更偏向毕业设计演示：登录后由前端保存用户信息，页面通过路由和菜单进行区分，后端在部分 Service 方法中校验操作者身份。若用于真实部署，还需要引入 JWT、Spring Security 或方法级权限控制，把登录态、接口权限和数据权限统一起来。",
    732: "匹配推荐算法目前采用固定权重。它的好处是容易解释，也便于在论文和页面中展示分项得分；不足是不同品类、地区和季节可能需要不同阈值。后续可以先积累撮合成功和失败记录，再根据真实成交情况调整名称、价格、时间等权重，而不是一开始就引入过重的推荐模型。",
    733: "消息通知方面，现有消息中心主要在站内展示审核结果、撮合状态和订单进度，用户需要登录系统后才能看到。后续可以增加 WebSocket 实时推送，或接入邮件、短信等通知方式，让供应商和采购商更快获知交易状态变化。",
    734: "移动端适配方面，当前页面主要服务桌面浏览器，表格和后台操作较多，在手机屏幕上使用并不方便。后续可改造响应式布局，或者为供应发布、需求浏览、撮合确认等高频操作单独设计移动端页面。",
    735: "数据分析方面，当前看板根据平台内部数据做统计和规则提示，不能直接完成市场价格预测或智能决策。后续若接入更完整的历史交易和外部市场数据，可以先从供需趋势、库存预警和价格区间分析做起，再逐步考虑预测模型或个性化推荐。",
    736: "部署运维方面，当前系统以前后端本地启动为主。后续可以把后端打包为独立 JAR 或容器镜像，前端构建为静态资源后由 Nginx 托管，再配合环境变量管理数据库连接和上传路径，使系统更接近实际部署形态。",
}


def set_paragraph_text_green(paragraph, text: str) -> None:
    """清空段落原有文字后写入绿色改写文本，保留段落样式和所在位置。"""
    first_run = paragraph.runs[0] if paragraph.runs else None
    paragraph.clear()
    run = paragraph.add_run(text)
    run.font.color.rgb = GREEN
    if first_run is not None:
        run.bold = first_run.bold
        run.italic = first_run.italic
        run.underline = first_run.underline
        run.font.size = first_run.font.size
        run.font.name = first_run.font.name


def main() -> None:
    WORK_DIR.mkdir(parents=True, exist_ok=True)
    if not DOCX_PATH.exists():
        raise FileNotFoundError(f"未找到工作稿：{DOCX_PATH}")

    timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
    backup_path = WORK_DIR / f"working_aigc_report_backup_{timestamp}.docx"
    shutil.copy2(DOCX_PATH, backup_path)

    document = Document(str(DOCX_PATH))
    changed: list[int] = []
    skipped: list[int] = []

    for index, replacement in REWRITES.items():
        if index >= len(document.paragraphs):
            skipped.append(index)
            continue
        paragraph = document.paragraphs[index]
        if not paragraph.text.strip():
            skipped.append(index)
            continue
        set_paragraph_text_green(paragraph, replacement)
        changed.append(index)

    revised_path = WORK_DIR / "免费_Word标红版_AIGC检测报告_[基于springboot+vue的农产品]_改稿版.docx"
    document.save(DOCX_PATH)
    shutil.copy2(DOCX_PATH, revised_path)

    REPORT_PATH.write_text(
        "\n".join(
            [
                "# AIGC 改稿说明",
                "",
                f"- 工作稿：{DOCX_PATH.resolve()}",
                f"- 输出稿：{revised_path.resolve()}",
                f"- 备份稿：{backup_path.resolve()}",
                f"- 本轮处理热点段落数：{len(changed)}",
                f"- 跳过段落序号：{skipped if skipped else '无'}",
                "",
                "## 本轮处理的热点",
                "",
                "- 红色高风险段：重点处理前言背景、技术可行性、社会可行性、总结展望中的固定化表述。",
                "- 橙色中风险段：处理摘要、研究意义、研究方法、开发环境、框架介绍、接口说明、系统设计、数据库设计、测试说明和总结段落。",
                "- 紫色提示段：处理传统交易背景、需求分析方法、核心表清单、推荐结果说明、供应审核、订单权限、测试环境等段落。",
                "",
                "## 改写策略",
                "",
                "- 将泛化政策、框架介绍和总结性句式改为与本项目代码、页面和数据库字段直接相关的表述。",
                "- 删除或弱化未实测的性能承诺，例如毫秒级响应、前端首屏时间等表述，改为开发环境和功能验证口径。",
                "- 保留原有图片、表格、题注和章节位置，没有凭空重画旧图表。",
                "- 被改写的正文段落统一标为绿色，便于和检测报告原色标注区分。",
                "",
                "## 下轮复检建议",
                "",
                "- 重新上传改稿版进行 AIGC 检测后，优先查看红色或橙色是否还集中在第 1、2、4、5、7 章。",
                "- 若仍有热点，下一轮只针对剩余页码继续改，不建议整篇重写。",
            ]
        ),
        encoding="utf-8",
    )

    print(f"changed={len(changed)}")
    print(f"backup={backup_path}")
    print(f"revised={revised_path}")
    print(f"report={REPORT_PATH}")


if __name__ == "__main__":
    main()
